# Validation

Tech.js integrates automatically with **ASP.NET Core jQuery Unobtrusive Validation**. When a form is invalid, the AJAX request is **not sent**.

This allows you to keep the standard ASP.NET Core validation workflow while using Tech.js for AJAX submissions.

---

# Requirements

Include the standard ASP.NET Core validation scripts:

```html id=
```
